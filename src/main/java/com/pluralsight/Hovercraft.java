package com.pluralsight;

public class Hovercraft extends Vehicle {
    public Hovercraft(String color, int numberOfPassengers, int cargoCapacity, int fuelCapacity, boolean isWorking, boolean isElectric,
                      boolean isTrackable, boolean isHotwireable, int stealthRating, int fuelEfficiency) {
        super(color, numberOfPassengers, cargoCapacity, fuelCapacity, isWorking, isElectric, isTrackable, isHotwireable, stealthRating, fuelEfficiency);
    }
}

